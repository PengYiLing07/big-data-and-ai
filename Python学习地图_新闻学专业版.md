# Python 四课时学习地图（新闻学专业定制版）

> **4 节课 × 45 分钟 = 180 分钟**
> 目标：不追求成为程序员，而是下课时手里有 4 个能跑的小脚本，并知道 Python 在新闻业务里该怎么用
> 课前准备约 40 分钟（不计入课时）｜ 课后作业每节约 30 分钟

---

## 开始之前，先说清三件事

**① 环境搭建必须放在课前，不能占用课堂时间**
安装 Python、装好 VS Code、跑通 `print("hello")`——请在第一节上课前完成（约 40 分钟，遇到问题提前找人或问 AI）。这是 45 分钟课时里最容易失控的一环。

**② 课堂上不追求"理解全部语法"，只追求"能跑出结果"**
遇到暂时看不懂的地方，先记在便签上，继续跟着敲。四节课的目标是建立直觉 + 拿到可用脚本。死磕一个细节，会挤掉整节课的动手时间。

**③ 每节课都必须当堂敲完，课后作业是"换自己的数据重跑"**
只看不敲等于没学。动手任务都控制在 15–20 分钟内可完成；课后作业不是新内容，而是把你自己的稿件、标题、表格替换进去再跑一遍。

---

## 四课时总览

| 课次 | 主题 | 产出脚本 | 时长 |
|---|---|---|---|
| 第 1 课 | 跑起来，会用变量和字符串 | 新闻导语速检器 `lesson1.py` | 45 min |
| 第 2 课 | 列表、字典、循环与判断 | 标题批量筛选器 `lesson2.py` | 45 min |
| 第 3 课 | 读写文件，读 CSV 做统计 | 采访记录统计脚本 `lesson3.py` | 45 min |
| 第 4 课 | 综合实战：词频分析与出图 | 词频排行 + 一张图 `lesson4.py` | 45 min |

每节课的课堂节奏统一为：**导入/复习 5 分 → 讲 + 跟敲 20 分 → 自己动手 15 分 → 小结 5 分**（第四课调整为 5 / 15 / 20 / 5）。

---

## 第 1 课（45 分钟）· 跑起来，会用变量和字符串

**难度 ★☆☆☆☆ ｜ 关键词：变量、数据类型、字符串、输入输出**
**节奏：导入 5 ｜ 讲 + 跟敲 20 ｜ 自己动手 15 ｜ 小结 5**

### 这节课只讲这四点
- **变量与命名**：把"标题""字数"这些概念装进变量里
- **四种基础类型**：文字 `str`、整数 `int`、小数 `float`、真假 `bool`
- **输出与输入**：`print()` 打印结果，`input()` 让人输入内容
- **字符串四件套**：`len()` 数长度、`split()` 切分、`replace()` 替换、`strip()` 去空白，外加 f-string 拼接

### 课堂示例代码

```python
# 新闻导语速检器
lead = "昨日下午，本市召开新闻发布会，就市民关心的公共交通调价问题作出回应。"
lead = lead.replace("\n", "").strip()

sentences = [s for s in lead.split("。") if s]

print(f"总字数：{len(lead)}")
print(f"句子数：{len(sentences)}")
print(f"平均句长：{len(lead)/len(sentences):.1f} 字")
```

### 课堂动手（15 分钟，必须当堂敲完）
把示例跑通，然后加两行：用 `input()` 读入一段导语，输出它的字数和句数。存成 `lesson1.py`。

### 课后作业（约 30 分钟）
拿你自己写的 3 段稿件导语各跑一次，记下平均句长。想一想：多少字算"句子太长"？判断标准由你定，代码只是替你数。

> **本节课刻意不讲**：列表、字典、循环、函数、面向对象——全部留到后面。

---

## 第 2 课（45 分钟）· 列表、字典、循环与判断

**难度 ★★☆☆☆ ｜ 关键词：列表、字典、for 循环、if 判断**
**节奏：复习 5 ｜ 讲 + 跟敲 20 ｜ 自己动手 15 ｜ 小结 5**

### 这节课只讲这四点
- **列表 list**：一次装一批标题，会取第几个（索引）、会数个数（`len`）、会排序（`sort`）
- **字典 dict**：装一条结构化线索——标题、来源、关键词各占一个字段
- **for 循环**：把列表里的每一条都拿出来看一遍
- **if 判断**：满足条件的留下，不满足的跳过；配合计数就是统计

### 课堂示例代码

```python
# 标题批量筛选器
titles = [
    "城市地铁票价调整方案公布",
    "老旧小区加装电梯提速",
    "地铁新线年底开通",
    "校园食堂价格引关注",
]

key = "地铁"
hit = 0

for t in titles:
    if key in t:
        hit = hit + 1
        print(f"命中：{t}")

print(f"共 {len(titles)} 条，其中 {hit} 条与「{key}」相关")
```

### 课堂动手（15 分钟，必须当堂敲完）
扩成 10 条自己的新闻标题，加入 `keywords` 列表（如 `["地铁", "小区", "学校"]`），用一个循环统计每个关键词各命中几条。存成 `lesson2.py`。

### 课后作业（约 30 分钟）
从今天看到的新闻里抄 10 条标题，跑一遍你的筛选器。感受一下：当标题变成 100 条时，它还能帮你省多少时间。

> **本节课刻意不讲**：`while` 循环、嵌套字典、列表推导式、函数定义。

---

## 第 3 课（45 分钟）· 读写文件与 CSV 统计

**难度 ★★★☆☆ ｜ 关键词：文件读写、CSV、编码、统计输出**
**节奏：复习 5 ｜ 讲 + 跟敲 18 ｜ 自己动手 17 ｜ 小结 5**

### 这节课只讲这四点
- **打开文件**：`with open("文件名", encoding="utf-8")`——唯一需要背下来的固定写法
- **读取表格**：用 `csv.DictReader` 把每行变成字典，字段名就是表头
- **统计**：还是上一课的 `for + if + 计数`，只是数据从手打变成了读文件
- **写回文件**：把统计结果存成新的 CSV，方便交给同事或放进稿件

### 课堂示例代码

```python
import csv

stat = {}

with open("采访记录.csv", encoding="utf-8") as f:
    reader = csv.DictReader(f)
    for row in reader:
        unit = row["单位"].strip()
        if unit:
            stat[unit] = stat.get(unit, 0) + 1

for unit, n in stat.items():
    print(f"{unit}　{n} 条")
```

### 课堂动手（17 分钟，必须当堂敲完）
用 20 行采访记录 CSV 跑通统计，把结果写进新文件 `统计结果.csv`。存成 `lesson3.py`。

### 课后作业（约 30 分钟）
把 CSV 换成你熟悉的真实表格（发稿台账、活动名单都可以），只改字段名，跑出自己的统计结果。顺便记一件事：中文乱码时，把 `encoding` 从 `utf-8` 改成 `gbk` 试试。

> **本节课刻意不讲**：正则表达式、`try/except` 异常处理、批量清洗规则、pandas。

---

## 第 4 课（45 分钟）· 综合实战：词频分析与出图

**难度 ★★★★☆ ｜ 关键词：第三方库、中文分词、词频统计、可视化**
**节奏：复习 5 ｜ 讲 + 跟敲 15 ｜ 自己动手 20 ｜ 小结 5**

### 这节课只讲这四点
- **安装第三方库**：一行 `pip install jieba`，从此能用别人写好的工具
- **中文分词**：`jieba.lcut()` 把标题切成一个个词，顺便过滤掉"的、了、在"这类无意义词
- **词频排行**：用 `Counter` 数出出现最多的词，取前 10
- **生成图表**：用 `matplotlib` 画柱状图，或用 `wordcloud` 生成词云（**二选一**即可，不贪多）

### 课堂示例代码

```python
import jieba
from collections import Counter

titles = ["..."]                      # 上一课的标题列表
stop = {"的", "了", "在", "和"}
words = []

for t in titles:
    for w in jieba.lcut(t):
        if len(w) > 1 and w not in stop:
            words.append(w)

for w, n in Counter(words).most_common(10):
    print(w, n)
```

### 课堂动手（20 分钟，结课产出）
准备 **30 条同一选题的新闻标题**，跑出词频 Top10，并生成一张柱状图或词云图。存成 `lesson4.py`，图片保存到本地。

### 结课小结（最后 5 分钟）
把生成的图展示给同学，用**一段话**说明：最突出的关键词是什么、可能意味着什么、这个结论有什么局限。
—— 结论比图表重要。图只是证据，讲清"所以然"才是新闻能力。

> **本节课刻意不讲**：网页抓取（`requests`）。它涉及请求头、反爬、合规等一堆问题，45 分钟内讲不透。先用自己收集或学校提供的标题文本完成分析。

---

## 四节课后：你真正掌握了什么

| 能力 | 状态 | 说明 |
|---|---|---|
| 看懂并改写一小段 Python | ✅ 能 | 拿到示例代码，知道该改哪几个地方（字段名、关键词、文件名） |
| 用列表和循环批量处理文本 | ✅ 能 | 几十到几百条标题的筛选、计数、排序，完全没问题 |
| 读写 CSV 并做基础统计 | ✅ 能 | 读取、按字段计数、输出新表格，日常台账类需求够用 |
| 做一次词频分析并出图 | ✅ 能 | 分词、排行、生成柱状图或词云，可放进稿件的配图 |
| 独立编写抓取网页的程序 | ⬜ 尚未 | 涉及请求头、反爬机制与合规判断，属于课后拓展方向 |
| 处理几万行以上的复杂数据 | ⬜ 尚未 | 需要进一步学 pandas，是自然的下一步 |
| 独立排查所有报错 | ⬜ 尚未 | 常见报错能凭经验处理，复杂报错建议直接问 AI |

---

## 给 45 分钟课堂的三条纪律

### 课上坚决不做的三件事
1. **不现场装环境** —— 课前 40 分钟必须解决，否则一节课就没了
2. **不追求理解全部语法** —— 先记下来，继续跟着敲
3. **不在课上查复杂问题** —— 卡住超过 3 分钟就举手，或先跳过

### 课后必须做的三件事
1. **把示例换成自己的数据重跑** —— 这是"学会"的唯一标志
2. **把代码文件存好** —— 四节课的脚本是你以后抄改的模板库
3. **每次报错都截图留存** —— 同一个坑踩两次才是浪费

### 新闻学生常见的四个坑
1. **只看不敲** —— 看懂了不等于会写，务必亲手敲一遍
2. **忽略缩进** —— Python 靠缩进划分代码块，空格和 Tab 混用必报错
3. **乱码就放弃** —— 多半是 `encoding` 该填 `gbk`
4. **怕报错** —— 从最后一行往上读，它才是原因

### 课后想继续学，按这个顺序
1. **函数 `def`** —— 把反复用的代码打包起来
2. **`pandas`** —— 处理真正的表格数据
3. **`requests`** —— 抓取网页 + 合规注意事项
4. **找一个真实选题** —— 完整做一次数据新闻

---

## 结课标准：180 分钟之后，你应该交得出什么

四节课结束时，请确认你手里有这四样东西——它们就是"学会基础运用"的全部证据：

1. **四个能跑的脚本文件**：`lesson1.py ~ lesson4.py`，每个对应一个真实新闻场景
2. **一份自己的数据分析结果**：用你熟悉的数据跑出来的统计或词频排行，不是示例数据
3. **一张能放进稿件的图**：柱状图或词云，来自第四课的结课产出
4. **一段 300 字以内的结论**：说清数据说明了什么、有什么局限

### 这四节课完全没有覆盖
- 函数与模块化、面向对象
- 正则表达式与复杂文本清洗
- pandas 数据框操作
- 网页抓取与反爬应对
- 网络爬取的合规边界判断

### 但你已经获得了更重要的东西
- 不再害怕代码和报错信息
- 知道 Python 能解决哪类新闻问题
- 有一套可以不断抄改的脚本模板库
- 能判断"这个问题该不该用代码解决"
- 一个可以继续自学的真实起点

---

## 这套材料之外还有什么

本地图是"压缩版"，只够 180 分钟。另外两份材料在同仓库根目录，用浏览器直接打开即可。

- **《Python 基础学习地图（13 次课版）》**（`Python学习地图_13次课版.html`）——把上面那份"完全没有覆盖"的清单展开成 13 次 × 90 分钟的完整排课，含每节课时间轴、当堂可跑的示例、产出文件名与课后作业。
- **概念资料库**（入口 `learning-materials/index.html`）——每个概念一页：通俗解释、可视化图解、易混与边界、5 道可点击自测题、可核查来源；目录页另有概念分布图、前置依赖表、跨课复用表。
- **使用建议**——先把这 180 分钟跑完，确认"代码能解决哪类新闻问题"；再按 13 次课版逐课推进；每节课前后用概念页做预习与复习。
